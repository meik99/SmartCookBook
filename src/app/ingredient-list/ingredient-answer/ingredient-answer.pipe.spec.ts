import { IngredientAnswerPipe } from './ingredient-answer.pipe';

describe('IngredientAnswerPipe', () => {
  it('create an instance', () => {
    const pipe = new IngredientAnswerPipe();
    expect(pipe).toBeTruthy();
  });
});
